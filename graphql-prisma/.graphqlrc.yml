projects:
  app:
    schema: http://localhost:4466
  database: 
    schema: prisma/prisma.yml
    extensions:
      codegen:
        generates:
          ./src/generated/schema.graphql:
            plugins:
              - schema-ast