import { Context } from '../common';
export declare function fromScratch({ context, templateName, templateUrl, }: {
    context: Context;
    templateName: string;
    templateUrl: string;
}): Promise<void>;
