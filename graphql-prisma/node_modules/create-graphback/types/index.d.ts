#!/usr/bin/env node
import * as yargs from 'yargs';
declare type Params = {
    name?: string;
    templateName?: string;
    templateUrl?: string;
    filter?: string;
};
export declare const desc = "Create Graphback project from available templates";
export declare const builder: (args: yargs.Argv) => void;
export declare function handler({ name, templateName, templateUrl, filter }: Params): Promise<void>;
export {};
