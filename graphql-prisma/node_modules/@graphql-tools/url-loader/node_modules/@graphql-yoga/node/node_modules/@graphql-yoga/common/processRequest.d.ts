import { RequestProcessContext } from './types';
export declare function processRequest<TContext, TRootValue = {}>({ contextFactory, execute, operationName, parse, query, request, schema, subscribe, validate, variables, extraHeaders, Response, ReadableStream, }: RequestProcessContext<TContext, TRootValue>): Promise<Response>;
